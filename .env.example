# Copiar a .env.local y ajustar segun el entorno.
# URL raiz del backend (panviz-backend) -- SIN /api/v1, los paths generados en
# src/api/schema.d.ts ya incluyen ese prefijo (viene asi del openapi.json).
VITE_API_BASE_URL=http://localhost:8000
